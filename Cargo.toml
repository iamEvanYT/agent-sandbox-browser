[package]
name = "agent-sandbox-browser"
version = "1.0.0"
edition = "2021"
description = "Dockerized Chromium sandbox for AI agent automation"

[[bin]]
name = "agent-sandbox-browser"
path = "src/main.rs"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
